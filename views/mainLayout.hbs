
{{> header}}
<div class="mainContainer">
    <div class="left-container commonBackgroundImage">
        {{> leftnav}}
    </div>
    <div class="content-container contentStyles">
        {{#if landingPage}}
            {{> uppy}}
        {{else}}
        {{{contentText}}}

        {{/if}}
    </div>
    <div class="right-container commonBackgroundImage">
        {{> rightRail}}
    </div>
</div>
